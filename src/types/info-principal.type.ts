export interface InfoPrincipalType {
    id?: string
    mainTitle: string
    year: number
    imageUrl: string
    color: {
        primary: string
        secondary: string
        accent: string
    }
}